''' definitions.py

    Contains definitions for the entire project
'''
from os.path import dirname, abspath

ROOT_DIR = dirname(abspath(__file__))
DB_DIR = ROOT_DIR + "/db/"
TESTS_DIR = ROOT_DIR + "/tests/"
TESTS_FIXTURES_DIR = TESTS_DIR + "fixtures/"
