matrix:
  include:
    - language: python
      python: 3.6
      install:
        - pip install -r backend/requirements.txt
      before_script:
        - cd backend/
      script:
        - py.test

notifications:
  email: never

after_success:
  coveralls
